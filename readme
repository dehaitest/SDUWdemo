# Exploration View

## How to use

Import `exploration_view` and refere to `example.py` to know how to integrate this component to Sapper. I wrote comments to each piece of the code.

Config your own OpenAI API key in `config.json`
